[mqtt]
# MQTT代理配置
broker = localhost
port = 1883
topic_prefix = sensor

[pir]
# PIR传感器配置
# BCM GPIO引脚编号，连接到传感器的OUT引脚
pin = 23
# 传感器类型名称
sensor_type = pir_motion
# 传感器稳定时间（秒） - HC-SR501通常需要60秒预热
stabilize_time = 60