[mqtt]
# MQTT代理配置
broker = localhost
port = 1883
# MQTT主题前缀，发布的消息主题格式为: {topic_prefix}/{sensor_type}
topic_prefix = sensor
# 数据发布间隔（秒）
publish_interval = 30

[dht22]
# DHT22传感器配置
pin = 4
# 传感器类型：DHT22 或 DHT11
sensor_type = DHT22
# 读取失败时的重试次数
retry_count = 3
# 重试间隔时间（秒）
retry_delay = 2 